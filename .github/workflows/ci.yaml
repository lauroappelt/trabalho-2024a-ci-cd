name: Continuos Integration

on:
  pull_request:
    braches:
      ['master']

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Build
        run: cp -rf ../../Dockerfile . && docker build -t hyperf .
